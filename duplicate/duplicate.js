const elements = ["a", "b", 1, 6, "a", "6", "c"]

const findDuplicate = () => {

    for(el of elements) {
        let elIndex = elements.indexOf(el)
        for(let i=0; i<elements.length; i++) {
            
        }

    }
   
}

findDuplicate()

